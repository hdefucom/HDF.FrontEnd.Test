{
  "name": "flow",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "flow && flow-remove-types src/ -d dist/ && echo ================================= && node dist/main.js",
    
    "flow": "flow",
    "babel": "babel src/ -d dist/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/cli": "^7.16.7",
    "@babel/core": "^7.16.7",
    "@babel/preset-flow": "^7.16.7",
    "flow-bin": "^0.168.0",
    "flow-remove-types": "^2.168.0"
  }
}